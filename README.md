# Laravel Products API

API для выборки товаров с поддержкой фильтрации и сортировки.

---

## 📦 Установка и запуск

1. **Клонируйте репозиторий**:
   ```bash
   git clone git@github.com:LioneNET/Isaev.git
   cd <папка-проекта>
   ```

2. **Установите зависимости**:
   Перейти в запущенный докер контейнер командой `docker exec -it laravel-php-fpm bash`
   ```bash
   composer install
   ```

3. **Создайте копию `.env` файла**:
   ```bash
   cp .env.example .env
   ```

4. **Сгенерируйте ключ приложения**:
   ```bash
   php artisan key:generate
   ```

5. **Настройте подключение к базе данных** в файле `.env`.

6. **Запустите миграции и сиды**:
   ```bash
   php artisan migrate --seed
   ```
   > Это создаст необходимые таблицы и заполнит их тестовыми данными.

---

## 📡 Доступные эндпоинты

### Получить список товаров

```http
GET /api/products
```

#### Параметры запроса (опционально)

| Параметр      | Тип    | Описание                                     |
|---------------|--------|----------------------------------------------|
| `name`        | string | Поиск по частичному совпадению названия      |
| `in_stock`    | bool   | Фильтр по наличию (`true`/`false`)           |
| `price_from`  | number | Минимальная цена                             |
| `price_to`    | number | Максимальная цена                            |
| `category_id` | int    | ID категории                                 |
| `rating_from` | number | Минимальный рейтинг                          |
| `sort`        | string | Сортировка: `price_asc`, `price_desc`, `rating_desc`, `newest` |

#### Пример запроса

```bash
curl "http://localhost:8000/api/products?in_stock=true&price_from=100&price_to=500&sort=price_asc"
```

#### Пример ответа

```json
[
  {
    "id": 1,
    "name": "Ноутбук HP Pavilion",
    "price": 45000,
    "category_id": 2,
    "in_stock": true,
    "rating": 4.7,
    "created_at": "15.01.2026",
    "updated_at": "16.01.2026"
  },
  {
    "id": 3,
    "name": "Мышь беспроводная",
    "price": 1200,
    "category_id": 1,
    "in_stock": true,
    "rating": 4.2,
    "created_at": "10.01.2026",
    "updated_at": "12.01.2026"
  }
]
```

---

## 🛠️ Технологии

- **Backend**: Laravel 12+
- **База данных**: PostgreSQL 14.9
- **Язык**: PHP 8.3